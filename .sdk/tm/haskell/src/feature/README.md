# Per-feature custom source

The Haskell target does not use per-feature custom source (entities and
features are generic and config-driven). These directories exist only so the
neutral feature scaffold has somewhere to look; they are excluded from Copy.
