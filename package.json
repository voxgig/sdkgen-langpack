{
  "name": "@voxgig/sdkgen-langpack",
  "version": "0.0.7",
  "description": "Additional language targets for the Voxgig SDK Generator: Dart, Haskell and Lean.",
  "type": "commonjs",
  "license": "MIT",
  "keywords": [
    "voxgig",
    "sdkgen",
    "sdkgen-package",
    "dart",
    "haskell",
    "lean"
  ],
  "author": "Richard Rodger (http://richardrodger.com)",
  "repository": {
    "type": "git",
    "url": "git://github.com/voxgig/sdkgen-langpack.git"
  },
  "files": [
    ".sdk",
    "sdkgen-package.json"
  ],
  "scripts": {
    "test": "node tools/comment-gate.cjs && node --enable-source-maps --test test/*.test.js",
    "test-some": "node --enable-source-maps --test-name-pattern=\"$npm_config_pattern\" --test test/*.test.js",
    "watch": "tsc -w",
    "build": "tsc",
    "clean": "rm -rf node_modules yarn.lock package-lock.json .build",
    "reset": "npm run clean && npm i && npm run build && npm test",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag v$REPO_VERSION && git push --tags;",
    "repo-publish": "npm run clean && npm i && npm run repo-publish-quick",
    "repo-publish-quick": "npm run build && npm run test && npm run repo-tag && npm publish --registry https://registry.npmjs.org --access=public",
    "resset": "npm run reset",
    "check-cmp": "tsc -p tsconfig.json --noEmit",
    "check-package": "voxgig-sdkgen package check ."
  },
  "_peer_comment": [
    "A PACKAGED TARGET INHERITS SDKGEN'S PEERS AS ITS OWN. All three targets'",
    "components import @voxgig/sdkgen, @voxgig/apidef and @voxgig/struct BY",
    "NAME, which resolved silently while they lived in the bundled scaffold",
    "beside sdkgen's own node_modules. Out here they have to be declared, or",
    "the components fail to type-check for the author and fail to resolve for",
    "anyone who installs this package without sdkgen's peers present.",
    "",
    "Peers rather than dependencies: a consumer necessarily has these already,",
    "via sdkgen, and a second copy at a different version is the one outcome",
    "worth preventing.",
    "",
    "VERIFIED AS REAL MODULE IMPORTS, not grepped. A generator's components",
    "are mostly strings containing other languages' source, so a text search",
    "cannot tell an import from a `require` line the component EMITS — that",
    "is how the seneca-provider migration nearly declared @seneca/maintain a",
    "peer. Each name here was traced to a top-level import statement:",
    "@voxgig/struct to utility_{dart,haskell,lean}.ts and TestEntity_dart.ts.",
    "",
    "THE SDKGEN FLOOR IS THE OLDEST PUBLISHED VERSION THIS SUITE IS RUN",
    "AGAINST, which is 4.23.0. Not the oldest that might work: a floor nobody",
    "tests is the shape of haskell's old >=3.5.0, which accepted versions",
    "where a target's components would install and then die at generation on",
    "a missing export.",
    "",
    "The build workflow's `floor` lane is what keeps that sentence true. It",
    "reads these two ranges out of peerDependencies and downgrades to them",
    "after install, so raising a floor here moves what CI exercises with it",
    "and there is no second copy of the number to drift. Keep",
    "engines.sdkgen in sdkgen-package.json in step with the range below; a",
    "consumer reads that one, and nothing asserts the two agree.",
    "",
    "Earlier floors, for the record: 4.9.0 was the first release carrying the",
    "test kit this suite runs on, and 4.10.0 the first carrying the secrets",
    "feature model that dart and lean both read."
  ],
  "peerDependencies": {
    "@voxgig/apidef": ">=8.14.0",
    "@voxgig/sdkgen": ">=4.23.0",
    "@voxgig/struct": ">=0.2"
  },
  "devDependencies": {
    "@types/node": ">=24",
    "@voxgig/apidef": ">=8.14.0",
    "@voxgig/sdkgen": ">=4.23.0",
    "@voxgig/struct": ">=0.2",
    "aontu": ">=0.71.0",
    "memfs": "4.68.1",
    "sucrase": "^3.35.1",
    "typescript": "7.0.2"
  }
}
